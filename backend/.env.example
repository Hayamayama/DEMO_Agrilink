PORT=3000
# Postgres (app role). Real value lives only in the server's backend/.env - never commit it.
# DATABASE_URL=postgresql://agrilink_app:<password>@127.0.0.1:5432/agrilink
# AI_PROVIDER=gemini
# GEMINI_API_KEY=
# DATA_GOV_IN_KEY=
